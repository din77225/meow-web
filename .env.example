# Build-time only. Fetches public video data from the YouTube Data API v3 during
# `npm run build`; never shipped to the browser. Set the same value in the
# Cloudflare Pages project settings. Restrict the key to YouTube Data API v3.
YOUTUBE_API_KEY=
